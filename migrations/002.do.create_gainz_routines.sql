CREATE TABLE gainz_routines (
  id INTEGER PRIMARY KEY GENERATED BY DEFAULT AS IDENTITY,
  routine_name TEXT NOT NULL,
  routine_owner INTEGER REFERENCES gainz_users(id)
    ON DELETE CASCADE NOT NULL
);