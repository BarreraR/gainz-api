CREATE TABLE gainz_routine_exercises (
  id INTEGER PRIMARY KEY GENERATED BY DEFAULT AS IDENTITY,
  routine_id INTEGER REFERENCES gainz_routines(id)
    ON DELETE CASCADE NOT NULL,
  exercise_id INTEGER REFERENCES gainz_exercises(id)
    ON DELETE CASCADE NOT NULL
);